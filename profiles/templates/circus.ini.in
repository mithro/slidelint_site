[watcher:worker]
cmd = ${worker} ${collector_chanel} ${producer_chanel} --slidelint=${slidelint_location} --onetime --logger=${buildout_dir}/worker_logging.conf
numprocesses = 6

[watcher:site]
cmd = ${buildout_dir}/bin/pserve ${buildout_dir}/.instance.ini
numprocesses = 1
